{
  "name": "@codesearch/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types and utilities for CodeSearch AI",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "tsc --watch --preserveWatchOutput",
    "build": "tsc",
    "test": "bun test",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "ioredis": "^5.4.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/bun": "^1.1.0"
  }
}
